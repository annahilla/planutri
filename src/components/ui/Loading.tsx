import { ClimbingBoxLoader } from "react-spinners";

const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen w-full">
      <ClimbingBoxLoader color="#545046" />
    </div>
  );
};
export default Loading;
